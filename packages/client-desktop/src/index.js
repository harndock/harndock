export const name = 'harndock-client-desktop-host'

// Client packages are Loader rows on both sides. The Node half is deliberately
// inert; product behavior lives in the browser half exported from ./client.
export function apply() {}
