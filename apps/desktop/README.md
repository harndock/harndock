# Desktop shell

This package is the Tauri 2 Bootstrap UI and Rust host for Harndock. During P0 it renders only the startup state; P1 adds the managed Harness Runtime lifecycle and navigation to the Runtime-served Web GUI.

Run commands from the repository root:

```sh
pnpm dev
pnpm build:web
pnpm check
```
